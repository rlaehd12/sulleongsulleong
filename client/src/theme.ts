import { createTheme } from '@mui/material';

const theme = createTheme({
	palette: {
		primary: {
			main: '#95E472',
			contrastText: '#fff',
		},
		info: {
			main: '#000',
			contrastText: '#000',
		},
	},
});

export default theme;
